# Shared UI IR

This directory documents the backend-neutral UI intermediate representation.
The current Python compiler still generates Python directly, but new backends
should target the concepts in `ui_schema.md`.
